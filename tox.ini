[tox]
envlist = py{39}

[testenv]
basepython =
    py39: python3.9
deps =
    check-manifest
    readme_renderer
    flake8
commands =
    check-manifest --ignore tox.ini,tests*
    python setup.py check -m -r -s
    flake8 .

[flake8]
exclude = .tox,*.egg,build,data
select = E,W,F
ignore=E501
